{
  "name": "meu-real-email-notifications",
  "private": true,
  "version": "19.0.0",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check src/index.js && node --check src/domain.js && node --check src/googleAuth.js && node --check src/firestore.js && node --check src/mailer.js",
    "validate": "npm run check && npm run test",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
